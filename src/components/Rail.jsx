import React from "react"

import "../assets/style/components/rail.scss"

const Rile  = ({children}) => (
    <section className="rail">
        
        {children}
        

    </section>

);

export default Rile