import React from "react"

import "../assets/style/components/rail.scss"

const Rile  = ({children}) => (
    <section className="rail">
        <div></div>
        {children}
        <div></div>

    </section>

);

export default Rile